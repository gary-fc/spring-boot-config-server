server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: develop
          uri: https://github.com/gary-fc/spring-boot-config-server
          search-paths: config-data
  application:
    name: config-service
